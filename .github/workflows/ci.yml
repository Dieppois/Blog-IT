name: CI/CD Blog IT

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch: 

jobs:
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Mettre à jour le système et installer Pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc

      - name: Construire le site statique
        run: |
          python3 scripts/build.py

      - name: Vérifier le contenu du dossier généré
        run: |
          echo "📂 Contenu du dossier site/"
          ls -R site || echo "⚠️ Aucun fichier trouvé dans site/"

      - name: Archiver le site généré
        uses: actions/upload-artifact@v4
        with:
          name: site
          path: site/

  test:
    name: ✅ Test
    runs-on: ubuntu-latest
    needs: build
  
    steps:
      - name: Télécharger le site généré
        uses: actions/download-artifact@v4
        with:
          name: site
          path: site
  
      - name: Vérifier la présence du fichier index.html
        run: |
          echo "📁 Contenu du dossier site/ après téléchargement :"
          ls -R site || echo "⚠️ Aucun fichier trouvé dans site/"
          echo "🔎 Vérification du fichier site/index.html"
          test -f site/index.html && echo "✅ Build OK" || (echo "❌ Fichier manquant" && exit 1)


  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Télécharger le site généré
        uses: actions/download-artifact@v4
        with:
          name: site
          path: site

      - name: Déploiement sur GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
